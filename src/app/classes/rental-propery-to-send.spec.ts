import { RentalProperyToSend } from './rental-propery-to-send';

describe('RentalProperyToSend', () => {
  it('should create an instance', () => {
    expect(new RentalProperyToSend()).toBeTruthy();
  });
});
